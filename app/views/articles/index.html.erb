<div class="container">
  <h1>Articles</h1>
  <div class="row">
   <% @articles.each do |article| %>
    <div class="col-md-12">
      <h2><%= link_to article.title, article %></h2>
      <p> <%= article.content %></p>
    </div>
    <% end %>
  </div>
</div>
